{% extends 'base.html.twig' %}

{% block title %}Hello TestController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <ul>


        {% for i in range("a","z") %}
            {{i|upper}}-
        {% endfor %}


       <p> {{ DateTime | date('d/m/Y') }}</p>

        {%  set tableau = ['1',2,'test'] %}
        {{ dump(tableau) }}
       <p> Random de tableau : {{ random(tableau) }} </p>
     {% if Tableaulanguages is not empty %}

        {{ dump(Tableaulanguages) }}

        {% for Valeurlanguages in Tableaulanguages %}
        <li>Languages : {{ Valeurlanguages.language }}</li>
        <li>Version : {{ Valeurlanguages.version }}</li><br>
        {% endfor %}
     {% else %}
         <p>Aucun languages</p>
      {% endif %}
    </ul>

    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'C:/Users/Stagiaire/CFA INSTA/ProjetRbnB/src/Controller/TestController.php'|file_link(0) }}">src/Controller/TestController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'C:/Users/Stagiaire/CFA INSTA/ProjetRbnB/templates/test/index.html.twig'|file_link(0) }}">templates/test/index.html.twig</a></code></li>
    </ul>
</div>
{% endblock %}
